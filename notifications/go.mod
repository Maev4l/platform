module github.com/Maev4l/platform/notifications

// 1.24 is the platform-wide floor: <= every consumer (meal-planner 1.25,
// users-management 1.25, alerter 1.26) so no consumer is forced to upgrade.
go 1.24
