// PLACEHOLDER — Task 16 will overwrite this with the real TopList implementation.
// Props accepted: { state, summary }
import { Card } from '@/components/ui/Card';

export const TopList = ({ state: _state, summary: _summary }) => (
  <Card className="flex flex-1 items-center justify-center p-4">
    <span className="font-mono text-xs text-muted-foreground">TopList — pending</span>
  </Card>
);
