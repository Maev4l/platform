// PLACEHOLDER — Task 15 will overwrite this with the real GeoMap implementation.
// Props accepted: { state, setState } (country filter drill-down)
import { Card } from '@/components/ui/Card';

export const GeoMap = ({ state: _state, setState: _setState }) => (
  <Card className="flex flex-1 items-center justify-center p-4">
    <span className="font-mono text-xs text-muted-foreground">GeoMap — pending</span>
  </Card>
);
