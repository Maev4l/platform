// PLACEHOLDER — Task 16 will overwrite this with the real StatusDonut implementation.
// Props accepted: { summary }
import { Card } from '@/components/ui/Card';

export const StatusDonut = ({ summary: _summary }) => (
  <Card className="flex items-center justify-center p-4">
    <span className="font-mono text-xs text-muted-foreground">StatusDonut — pending</span>
  </Card>
);
