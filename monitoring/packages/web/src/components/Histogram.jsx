// PLACEHOLDER — Task 15 will overwrite this with the real Histogram implementation.
// Props accepted: { state }
import { Card } from '@/components/ui/Card';

export const Histogram = ({ state: _state }) => (
  <Card className="flex items-center justify-center p-4">
    <span className="font-mono text-xs text-muted-foreground">Histogram — pending</span>
  </Card>
);
