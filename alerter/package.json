{
  "name": "@platform/alerter",
  "version": "1.0.0",
  "scripts": {
    "backend:deploy": "make -C function package && yarn infra:apply",
    "infra:plan": "terraform -chdir=infrastructure plan",
    "infra:apply": "terraform -chdir=infrastructure apply -auto-approve"
  }
}
